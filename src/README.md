# Source code status

The thesis reproducibility manifest identifies an original end-to-end `analysis_pipeline.py` and an `analysis_config.yaml` used for the final analysis.

Those exact original files are not currently available in this chat session. They have **not** been regenerated from the dissertation text because doing so would create a new implementation rather than preserve the exact code used to produce the reported results.

Before the archival/Zenodo release, the authors should copy the exact original analysis code from the machine/directory used for the final thesis run, then review it for:

- local file paths;
- credentials or tokens;
- respondent-level data embedded in code/output;
- restricted DHS filenames or content that should not be redistributed.

Once reviewed, the original code can be added here and this status note updated.
